# dFactory/attic — archived

Off the live training path. See `../README.md` for what IS live.

- **`smoke_dbet.py`** — off-cluster smoke test that builds a tiny DBet model + runs a train step (validated the
  pipeline before real runs). Imports `tasks/dbet_train_core.py`; fix the import path if resurrected.
